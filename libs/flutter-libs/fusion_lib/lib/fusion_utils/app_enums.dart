/// Product types enum
enum ProductType {
  speaker,
  endpoints,
  amplifier,
  sources,
  dsps,
  controllers,
  racks,
}

/// Sort options enum
enum SortOption {
  priceHighToLow,
  priceLowToHigh,
  nameAToZ,
  nameZToA,
}

/// Coverage levels enum
enum CoverageLevel {
  low,
  mid,
  high,
}

/// Impedance levels enum
enum ImpedanceLevel {
  low,
  high,
}
