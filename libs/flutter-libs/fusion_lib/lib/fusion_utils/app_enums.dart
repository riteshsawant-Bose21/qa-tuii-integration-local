/// Product types enum
enum ProductType {
  speaker,
  amplifier,
  endpoints,
  sources,
  dsps,
  controllers,
}

/// Sort options enum
enum SortOption {
  priceHighToLow,
  priceLowToHigh,
  nameAToZ,
  nameZToA,
}

/// Coverage levels enum
enum CoverageLevel {
  low,
  mid,
  high,
}

/// Impedance levels enum
enum ImpedanceLevel {
  low,
  high,
}
