project:
  name: 'Fusion DSP'
  description: 'Fusion DSP application moon tasks'

tasks:

  build-embedded-interface-shared-library:
    command: "./moon-build.sh"
    deps:
      - ready-script

  ready-script:
    command: "chmod"
    args: ["+x", "moon-build.sh"]