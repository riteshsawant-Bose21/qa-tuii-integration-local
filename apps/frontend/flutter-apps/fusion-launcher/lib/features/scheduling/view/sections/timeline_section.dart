part of '../scheduling_page.dart';

class _TimelineSection extends StatelessWidget {
  const _TimelineSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Timeline Section"));
  }
}
