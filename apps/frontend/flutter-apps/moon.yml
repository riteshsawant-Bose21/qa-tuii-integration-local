project:
  name: "flutter-apps"
  description: "Flutter applications workspace"

tasks:
  # Batch build
  build-all:
    deps:
      - "fusion-launcher:build-apk"
      - "fusion-launcher:build-ios"
      - "fusion-launcher:build-macos"

