project:
  name: "react-apps"
  description: "React applications workspace"

tasks:
  # Batch build
  build-all:
    deps:
      - "landing-page:build"
      - "web-dashboard:build"
      - "fusion-cloud:build"
