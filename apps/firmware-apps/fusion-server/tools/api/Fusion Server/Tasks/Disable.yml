info:
  name: Disable
  type: http
  seq: 7

http:
  method: POST
  url: "{{api_url}}/tasks/:id/disable"
  params:
    - name: id
      value: analog-input
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
