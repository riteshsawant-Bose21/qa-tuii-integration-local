info:
  name: Update Scene Snapshot Task
  type: http
  seq: 13

http:
  method: PATCH
  url: "{{api_url}}/tasks/:id"
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: id
      value: scene-snapshot-task-001
      type: path
  body:
    type: json
    data: |-
      {
        "description": "Activate alternate scene snapshot definition",
        "snapshot_definition_id": "snapshot-test-02"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Updates a scene snapshot task using the protobuf JSON request shape.
