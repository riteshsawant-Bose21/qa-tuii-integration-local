info:
  name: Get Audio Block Settings
  type: http
  seq: 2

http:
  method: GET
  url: "{{api_url}}/settings/audio/:blockId"
  params:
    - name: blockId
      value: PEQ4385417
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
