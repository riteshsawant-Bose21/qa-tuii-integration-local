info:
  name: Get Audio Param
  type: http
  seq: 3

http:
  method: GET
  url: "{{api_url}}/settings/audio/:blockId/:param"
  params:
    - name: blockId
      value: GAIN528578446
      type: path
    - name: param
      value: gain
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
