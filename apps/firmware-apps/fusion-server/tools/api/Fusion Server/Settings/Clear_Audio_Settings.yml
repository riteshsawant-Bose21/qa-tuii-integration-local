info:
  name: Clear Audio Settings
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{api_url}}/settings/audio"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
