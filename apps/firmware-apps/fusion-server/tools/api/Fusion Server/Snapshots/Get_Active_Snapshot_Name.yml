info:
  name: Get Active Snapshot Name
  type: http
  seq: 6

http:
  method: GET
  url: "{{api_url}}/snapshots/meta/active"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
