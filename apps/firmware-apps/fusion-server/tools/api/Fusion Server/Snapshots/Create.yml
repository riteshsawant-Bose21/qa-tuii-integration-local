info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{api_url}}/snapshots/:name"
  params:
    - name: name
      value: bleep13
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
