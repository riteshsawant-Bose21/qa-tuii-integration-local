info:
  name: Get
  type: http
  seq: 2

http:
  method: GET
  url: "{{api_url}}/snapshots/:name"
  params:
    - name: name
      value: persist_data_1764285558388908000
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
