info:
  name: Delete
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{api_url}}/snapshots/:name"
  params:
    - name: name
      value: ""
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
