info:
  name: Get Active
  type: http
  seq: 2

http:
  method: GET
  url: "{{api_url}}/time-machine/meta/active"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Returns the active time-machine snapshot name.
