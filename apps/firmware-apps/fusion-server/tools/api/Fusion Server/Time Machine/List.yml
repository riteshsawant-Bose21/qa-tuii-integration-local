info:
  name: List
  type: http
  seq: 1

http:
  method: GET
  url: "{{api_url}}/time-machine"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Lists available time-machine snapshots.
