info:
  name: Delete
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{api_url}}/time-machine/:name"
  params:
    - name: name
      value: tm-test-01
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Deletes a stored time-machine snapshot.
