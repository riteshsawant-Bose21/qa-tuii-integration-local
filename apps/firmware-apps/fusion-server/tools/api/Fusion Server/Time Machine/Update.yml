info:
  name: Update
  type: http
  seq: 6

http:
  method: POST
  url: "{{api_url}}/time-machine/update/:name"
  params:
    - name: name
      value: tm-test-01
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Overwrites a stored time-machine snapshot with the current active state.
