info:
  name: Get By Name
  type: http
  seq: 3

http:
  method: GET
  url: "{{api_url}}/time-machine/:name"
  params:
    - name: name
      value: default
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Returns the typed time-machine snapshot payload for one snapshot name.
