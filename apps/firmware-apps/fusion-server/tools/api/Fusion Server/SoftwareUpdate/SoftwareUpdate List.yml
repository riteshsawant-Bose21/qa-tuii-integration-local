info:
  name: SoftwareUpdate List
  type: http
  seq: 2

http:
  method: GET
  url: "{{api_url}}/softwareUpdate/list"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
