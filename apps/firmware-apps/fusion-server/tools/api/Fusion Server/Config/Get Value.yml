info:
  name: Get Value
  type: http
  seq: 1

http:
  method: GET
  url: "{{api_url}}/value?key=settings"
  params:
    - name: key
      value: settings
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
