info:
  name: Get VIP Reload Status
  type: http
  seq: 10

http:
  method: GET
  url: "{{admin_url}}/device/reload/vip/status"
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
