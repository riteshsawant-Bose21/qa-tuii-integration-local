info:
  name: Cluster Network Latency Local
  type: http
  seq: 1

http:
  method: GET
  url: "{{admin_url}}/cluster/latency/network/local"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
