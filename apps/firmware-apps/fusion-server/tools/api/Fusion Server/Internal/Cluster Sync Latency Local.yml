info:
  name: Cluster Sync Latency Local
  type: http
  seq: 5

http:
  method: GET
  url: "{{admin_url}}/cluster/latency/sync/local"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
