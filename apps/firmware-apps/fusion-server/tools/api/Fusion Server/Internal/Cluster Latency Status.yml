info:
  name: Cluster Latency Status
  type: http
  seq: 2

http:
  method: GET
  url: "{{admin_url}}/cluster/latency/status/local"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
