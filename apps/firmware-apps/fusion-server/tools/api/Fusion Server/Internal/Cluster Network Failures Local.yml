info:
  name: Cluster Network Failures Local
  type: http
  seq: 4

http:
  method: GET
  url: "{{admin_url}}/cluster/latency/network/failures/local"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
