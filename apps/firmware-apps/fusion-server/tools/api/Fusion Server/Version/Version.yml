info:
  name: Version
  type: http
  seq: 1

http:
  method: GET
  url: "{{api_url}}/version"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
