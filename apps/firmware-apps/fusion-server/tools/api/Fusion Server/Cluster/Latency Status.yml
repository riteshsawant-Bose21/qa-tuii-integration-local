info:
  name: Latency Status
  type: http
  seq: 2

http:
  method: GET
  url: "{{metrics_url}}/cluster/latency/status"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
