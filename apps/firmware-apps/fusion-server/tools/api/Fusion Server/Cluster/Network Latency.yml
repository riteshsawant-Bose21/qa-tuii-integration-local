info:
  name: Network Latency
  type: http
  seq: 4

http:
  method: GET
  url: "{{metrics_url}}/cluster/latency/network"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
