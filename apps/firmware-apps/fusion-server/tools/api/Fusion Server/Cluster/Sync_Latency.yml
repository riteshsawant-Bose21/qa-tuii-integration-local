info:
  name: Sync Latency
  type: http
  seq: 6

http:
  method: GET
  url: "{{metrics_url}}/cluster/latency/sync"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
