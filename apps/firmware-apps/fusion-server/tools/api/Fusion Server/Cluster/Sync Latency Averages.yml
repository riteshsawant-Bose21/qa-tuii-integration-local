info:
  name: Sync Latency Averages
  type: http
  seq: 7

http:
  method: GET
  url: "{{metrics_url}}/cluster/latency/sync/averages"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
