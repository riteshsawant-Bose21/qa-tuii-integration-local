info:
  name: Network Failures
  type: http
  seq: 3

http:
  method: GET
  url: "{{metrics_url}}/cluster/latency/network/failures"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
