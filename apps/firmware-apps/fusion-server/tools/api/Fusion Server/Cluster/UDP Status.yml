info:
  name: UDP Status
  type: http
  seq: 9

http:
  method: GET
  url: "{{metrics_url}}/cluster/udp/status"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs:
  content: |
    Returns UDP diagnostics status for the node.

    Note: this endpoint is available only when `FUSION_UDP_DIAGNOSTICS=1` is enabled on fusion-server.
  type: text/markdown
