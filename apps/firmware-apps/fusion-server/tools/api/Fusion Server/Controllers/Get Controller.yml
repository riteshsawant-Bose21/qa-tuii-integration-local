info:
  name: Get Controller
  type: http
  seq: 2

http:
  method: GET
  url: "{{api_url}}/controllers"
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
