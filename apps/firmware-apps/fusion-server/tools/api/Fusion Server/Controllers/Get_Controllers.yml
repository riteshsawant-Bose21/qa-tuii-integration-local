info:
  name: Get Controllers
  type: http
  seq: 1

http:
  method: GET
  url: "{{api_url}}/controllers/:id"
  params:
    - name: id
      value: "1234"
      type: path
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
