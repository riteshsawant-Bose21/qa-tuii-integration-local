info:
  name: List Scene Sets From Node
  type: http
  seq: 17

http:
  method: GET
  url: "{{node_url}}/scenes-sets"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Replication check. Lists scene sets from a specific node URL.
