info:
  name: Get Feature Probe Value
  type: http
  seq: 9

http:
  method: GET
  url: "{{api_url}}/value?key=feature_probe"
  params:
    - name: key
      value: feature_probe
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Reads the current feature_probe branch from active state, useful after snapshot/scene activation.
