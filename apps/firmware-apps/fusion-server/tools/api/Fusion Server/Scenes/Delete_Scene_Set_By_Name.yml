info:
  name: Delete Scene Set By Name
  type: http
  seq: 17

http:
  method: DELETE
  url: "{{api_url}}/scenes-sets/:id"
  params:
    - name: id
      value: scene-set-test-01
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Deletes one scene set by set_id.
