info:
  name: List Scene Sets
  type: http
  seq: 7

http:
  method: GET
  url: "{{api_url}}/scene-sets"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Returns all scene sets with their full scene lists.
