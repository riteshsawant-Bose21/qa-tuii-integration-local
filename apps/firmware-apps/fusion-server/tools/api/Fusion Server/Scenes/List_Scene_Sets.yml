info:
  name: List Scenes Sets
  type: http
  seq: 4

http:
  method: GET
  url: "{{api_url}}/scenes-sets"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Returns all scene sets with their full scene lists.
