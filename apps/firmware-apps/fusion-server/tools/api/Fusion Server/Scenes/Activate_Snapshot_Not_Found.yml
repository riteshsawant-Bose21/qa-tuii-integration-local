info:
  name: Activate Snapshot Not Found
  type: http
  seq: 11

http:
  method: POST
  url: "{{api_url}}/snapshots/activate/does-not-exist"
  body:
    type: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Negative-path test. Activating a missing snapshot should return not found.
