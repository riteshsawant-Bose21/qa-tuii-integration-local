info:
  name: List Snapshots From Node
  type: http
  seq: 16

http:
  method: GET
  url: "{{node_url}}/snapshots"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Replication check. Lists snapshots from a specific node URL.
