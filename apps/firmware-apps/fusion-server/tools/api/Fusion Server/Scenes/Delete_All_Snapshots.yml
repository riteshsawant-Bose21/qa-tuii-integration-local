info:
  name: Delete All Snapshots
  type: http
  seq: 14

http:
  method: DELETE
  url: "{{api_url}}/snapshots"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Deletes all snapshot definitions.
