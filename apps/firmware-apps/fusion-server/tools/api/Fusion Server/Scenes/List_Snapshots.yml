info:
  name: List Snapshots
  type: http
  seq: 2

http:
  method: GET
  url: "{{api_url}}/snapshots"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Returns all snapshot definitions stored in the state.
