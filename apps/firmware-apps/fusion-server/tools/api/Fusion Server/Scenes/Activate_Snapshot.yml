info:
  name: Activate Snapshot
  type: http
  seq: 11

http:
  method: POST
  url: "{{api_url}}/snapshots/activate/:id"
  params:
    - name: id
      value: snapshot-test-01
      type: path
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Patches the active state with the data block from the named snapshot definition. Does not bump the epoch.
