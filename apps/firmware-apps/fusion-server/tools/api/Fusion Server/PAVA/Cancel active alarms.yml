info:
  name: Cancel active alarms
  type: http
  seq: 13

http:
  method: POST
  url: "{{api_url}}/pava/cancel"
  body:
    type: json
    data: |-
      {
        "zones": []
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
