info:
  name: List audio tags
  type: http
  seq: 8

http:
  method: GET
  url: "{{api_url}}/pava/messages/tags"
  params:
    - name: tag
      value: Emergencies
      type: query
      disabled: true
    - name: tag
      value: Fire+Drill
      type: query
      disabled: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
