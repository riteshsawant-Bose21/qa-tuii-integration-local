info:
  name: Get system diagnostics
  type: http
  seq: 12

http:
  method: GET
  url: "{{api_url}}/pava/diagnostics"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
