info:
  name: List scheduled messages
  type: http
  seq: 14

http:
  method: GET
  url: "{{api_url}}/pava/schedule"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
