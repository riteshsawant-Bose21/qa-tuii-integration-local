info:
  name: List Scene Catalog
  type: http
  seq: 5

http:
  method: GET
  url: "{{api_url}}/scene-catalog-list"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Returns a combined view of all snapshots and scene sets - the full scene catalog.
