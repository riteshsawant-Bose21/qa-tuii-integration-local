info:
  name: List Scenes
  type: http
  seq: 3

http:
  method: GET
  url: "{{api_url}}/scenes/list"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Returns all individual scenes across all scene sets.
