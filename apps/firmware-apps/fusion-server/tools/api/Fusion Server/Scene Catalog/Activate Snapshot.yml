info:
  name: Activate Snapshot
  type: http
  seq: 6

http:
  method: POST
  url: "{{api_url}}/snapshots/activate"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "id": "snapshot-test-01"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Patches the active state with the data block from the named snapshot definition. Does not bump the epoch.
