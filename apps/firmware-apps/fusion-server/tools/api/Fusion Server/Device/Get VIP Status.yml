info:
  name: Get VIP Status
  type: http
  seq: 5

http:
  method: GET
  url: "{{api_url}}/devices/vip/status"
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
