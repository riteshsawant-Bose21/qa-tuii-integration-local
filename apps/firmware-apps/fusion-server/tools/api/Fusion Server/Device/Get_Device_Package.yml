info:
  name: Get Device Package
  type: http
  seq: 3

http:
  method: GET
  url: "{{api_url}}/device"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
