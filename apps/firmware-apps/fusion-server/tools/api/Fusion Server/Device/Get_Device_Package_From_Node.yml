info:
  name: Get Device Package From Node
  type: http
  seq: 5

http:
  method: GET
  url: "{{node_url}}/device"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
