info:
  name: Set VIP master priority
  type: http
  seq: 6

http:
  method: POST
  url: "{{api_url}}/devices/:id/vip/master-priority/:mode"
  params:
    - name: mode
      value: low
      type: path
    - name: id
      value: fusion_dgoa3a0nspye_gg07_instance
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
