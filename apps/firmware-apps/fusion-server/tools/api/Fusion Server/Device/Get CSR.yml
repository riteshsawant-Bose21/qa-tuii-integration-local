info:
  name: Get CSR
  type: http
  seq: 5

http:
  method: GET
  url: "{{api_url}}/devices/:id/csr"
  auth: inherit
  params:
    id: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
