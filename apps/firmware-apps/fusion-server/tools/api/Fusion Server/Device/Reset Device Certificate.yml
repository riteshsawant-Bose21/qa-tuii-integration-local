info:
  name: Reset Device Certificate
  type: http
  seq: 7

http:
  method: delete
  url: "{{api_url}}/devices/:id/reset"
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
