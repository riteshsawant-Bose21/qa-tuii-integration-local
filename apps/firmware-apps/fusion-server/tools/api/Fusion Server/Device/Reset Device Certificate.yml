info:
  name: Reset Device Certificate
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{api_url}}/devices/:id/reset"
  auth: inherit
  params:
    id: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
