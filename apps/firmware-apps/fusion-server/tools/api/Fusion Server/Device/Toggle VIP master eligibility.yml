info:
  name: Toggle VIP master eligibility
  type: http
  seq: 6

http:
  method: POST
  url: "{{api_url}}/devices/:id/vip/master-eligibility/:enabled"
  params:
    - name: enabled
      value: "false"
      type: path
    - name: id
      value: fusion_dgoa3a0nspye_gg07_instance
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
