// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// AccountsType is an object representing the database table.
type AccountsType struct {
	ID   string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	Name null.String `boil:"name" json:"name,omitempty" toml:"name" yaml:"name,omitempty"`

	R *accountsTypeR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L accountsTypeL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var AccountsTypeColumns = struct {
	ID   string
	Name string
}{
	ID:   "id",
	Name: "name",
}

var AccountsTypeTableColumns = struct {
	ID   string
	Name string
}{
	ID:   "accounts_type.id",
	Name: "accounts_type.name",
}

// Generated where

var AccountsTypeWhere = struct {
	ID   whereHelperstring
	Name whereHelpernull_String
}{
	ID:   whereHelperstring{field: "\"accounts_type\".\"id\""},
	Name: whereHelpernull_String{field: "\"accounts_type\".\"name\""},
}

// AccountsTypeRels is where relationship names are stored.
var AccountsTypeRels = struct {
}{}

// accountsTypeR is where relationships are stored.
type accountsTypeR struct {
}

// NewStruct creates a new relationship struct
func (*accountsTypeR) NewStruct() *accountsTypeR {
	return &accountsTypeR{}
}

// accountsTypeL is where Load methods for each relationship are stored.
type accountsTypeL struct{}

var (
	accountsTypeAllColumns            = []string{"id", "name"}
	accountsTypeColumnsWithoutDefault = []string{"id"}
	accountsTypeColumnsWithDefault    = []string{"name"}
	accountsTypePrimaryKeyColumns     = []string{"id"}
	accountsTypeGeneratedColumns      = []string{}
)

type (
	// AccountsTypeSlice is an alias for a slice of pointers to AccountsType.
	// This should almost always be used instead of []AccountsType.
	AccountsTypeSlice []*AccountsType
	// AccountsTypeHook is the signature for custom AccountsType hook methods
	AccountsTypeHook func(context.Context, boil.ContextExecutor, *AccountsType) error

	accountsTypeQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	accountsTypeType                 = reflect.TypeOf(&AccountsType{})
	accountsTypeMapping              = queries.MakeStructMapping(accountsTypeType)
	accountsTypePrimaryKeyMapping, _ = queries.BindMapping(accountsTypeType, accountsTypeMapping, accountsTypePrimaryKeyColumns)
	accountsTypeInsertCacheMut       sync.RWMutex
	accountsTypeInsertCache          = make(map[string]insertCache)
	accountsTypeUpdateCacheMut       sync.RWMutex
	accountsTypeUpdateCache          = make(map[string]updateCache)
	accountsTypeUpsertCacheMut       sync.RWMutex
	accountsTypeUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var accountsTypeAfterSelectMu sync.Mutex
var accountsTypeAfterSelectHooks []AccountsTypeHook

var accountsTypeBeforeInsertMu sync.Mutex
var accountsTypeBeforeInsertHooks []AccountsTypeHook
var accountsTypeAfterInsertMu sync.Mutex
var accountsTypeAfterInsertHooks []AccountsTypeHook

var accountsTypeBeforeUpdateMu sync.Mutex
var accountsTypeBeforeUpdateHooks []AccountsTypeHook
var accountsTypeAfterUpdateMu sync.Mutex
var accountsTypeAfterUpdateHooks []AccountsTypeHook

var accountsTypeBeforeDeleteMu sync.Mutex
var accountsTypeBeforeDeleteHooks []AccountsTypeHook
var accountsTypeAfterDeleteMu sync.Mutex
var accountsTypeAfterDeleteHooks []AccountsTypeHook

var accountsTypeBeforeUpsertMu sync.Mutex
var accountsTypeBeforeUpsertHooks []AccountsTypeHook
var accountsTypeAfterUpsertMu sync.Mutex
var accountsTypeAfterUpsertHooks []AccountsTypeHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *AccountsType) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountsTypeAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *AccountsType) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountsTypeBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *AccountsType) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountsTypeAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *AccountsType) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountsTypeBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *AccountsType) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountsTypeAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *AccountsType) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountsTypeBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *AccountsType) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountsTypeAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *AccountsType) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountsTypeBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *AccountsType) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountsTypeAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddAccountsTypeHook registers your hook function for all future operations.
func AddAccountsTypeHook(hookPoint boil.HookPoint, accountsTypeHook AccountsTypeHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		accountsTypeAfterSelectMu.Lock()
		accountsTypeAfterSelectHooks = append(accountsTypeAfterSelectHooks, accountsTypeHook)
		accountsTypeAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		accountsTypeBeforeInsertMu.Lock()
		accountsTypeBeforeInsertHooks = append(accountsTypeBeforeInsertHooks, accountsTypeHook)
		accountsTypeBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		accountsTypeAfterInsertMu.Lock()
		accountsTypeAfterInsertHooks = append(accountsTypeAfterInsertHooks, accountsTypeHook)
		accountsTypeAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		accountsTypeBeforeUpdateMu.Lock()
		accountsTypeBeforeUpdateHooks = append(accountsTypeBeforeUpdateHooks, accountsTypeHook)
		accountsTypeBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		accountsTypeAfterUpdateMu.Lock()
		accountsTypeAfterUpdateHooks = append(accountsTypeAfterUpdateHooks, accountsTypeHook)
		accountsTypeAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		accountsTypeBeforeDeleteMu.Lock()
		accountsTypeBeforeDeleteHooks = append(accountsTypeBeforeDeleteHooks, accountsTypeHook)
		accountsTypeBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		accountsTypeAfterDeleteMu.Lock()
		accountsTypeAfterDeleteHooks = append(accountsTypeAfterDeleteHooks, accountsTypeHook)
		accountsTypeAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		accountsTypeBeforeUpsertMu.Lock()
		accountsTypeBeforeUpsertHooks = append(accountsTypeBeforeUpsertHooks, accountsTypeHook)
		accountsTypeBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		accountsTypeAfterUpsertMu.Lock()
		accountsTypeAfterUpsertHooks = append(accountsTypeAfterUpsertHooks, accountsTypeHook)
		accountsTypeAfterUpsertMu.Unlock()
	}
}

// One returns a single accountsType record from the query.
func (q accountsTypeQuery) One(ctx context.Context, exec boil.ContextExecutor) (*AccountsType, error) {
	o := &AccountsType{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for accounts_type")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all AccountsType records from the query.
func (q accountsTypeQuery) All(ctx context.Context, exec boil.ContextExecutor) (AccountsTypeSlice, error) {
	var o []*AccountsType

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to AccountsType slice")
	}

	if len(accountsTypeAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all AccountsType records in the query.
func (q accountsTypeQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count accounts_type rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q accountsTypeQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if accounts_type exists")
	}

	return count > 0, nil
}

// AccountsTypes retrieves all the records using an executor.
func AccountsTypes(mods ...qm.QueryMod) accountsTypeQuery {
	mods = append(mods, qm.From("\"accounts_type\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"accounts_type\".*"})
	}

	return accountsTypeQuery{q}
}

// FindAccountsType retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindAccountsType(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*AccountsType, error) {
	accountsTypeObj := &AccountsType{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"accounts_type\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, accountsTypeObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from accounts_type")
	}

	if err = accountsTypeObj.doAfterSelectHooks(ctx, exec); err != nil {
		return accountsTypeObj, err
	}

	return accountsTypeObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *AccountsType) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no accounts_type provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(accountsTypeColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	accountsTypeInsertCacheMut.RLock()
	cache, cached := accountsTypeInsertCache[key]
	accountsTypeInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			accountsTypeAllColumns,
			accountsTypeColumnsWithDefault,
			accountsTypeColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(accountsTypeType, accountsTypeMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(accountsTypeType, accountsTypeMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"accounts_type\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"accounts_type\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into accounts_type")
	}

	if !cached {
		accountsTypeInsertCacheMut.Lock()
		accountsTypeInsertCache[key] = cache
		accountsTypeInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the AccountsType.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *AccountsType) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	accountsTypeUpdateCacheMut.RLock()
	cache, cached := accountsTypeUpdateCache[key]
	accountsTypeUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			accountsTypeAllColumns,
			accountsTypePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update accounts_type, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"accounts_type\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, accountsTypePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(accountsTypeType, accountsTypeMapping, append(wl, accountsTypePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update accounts_type row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for accounts_type")
	}

	if !cached {
		accountsTypeUpdateCacheMut.Lock()
		accountsTypeUpdateCache[key] = cache
		accountsTypeUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q accountsTypeQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for accounts_type")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for accounts_type")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o AccountsTypeSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), accountsTypePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"accounts_type\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, accountsTypePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in accountsType slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all accountsType")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *AccountsType) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("models: no accounts_type provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(accountsTypeColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	accountsTypeUpsertCacheMut.RLock()
	cache, cached := accountsTypeUpsertCache[key]
	accountsTypeUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			accountsTypeAllColumns,
			accountsTypeColumnsWithDefault,
			accountsTypeColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			accountsTypeAllColumns,
			accountsTypePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert accounts_type, could not build update column list")
		}

		ret := strmangle.SetComplement(accountsTypeAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(accountsTypePrimaryKeyColumns) == 0 {
				return errors.New("models: unable to upsert accounts_type, could not build conflict column list")
			}

			conflict = make([]string, len(accountsTypePrimaryKeyColumns))
			copy(conflict, accountsTypePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"accounts_type\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(accountsTypeType, accountsTypeMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(accountsTypeType, accountsTypeMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert accounts_type")
	}

	if !cached {
		accountsTypeUpsertCacheMut.Lock()
		accountsTypeUpsertCache[key] = cache
		accountsTypeUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single AccountsType record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *AccountsType) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no AccountsType provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), accountsTypePrimaryKeyMapping)
	sql := "DELETE FROM \"accounts_type\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from accounts_type")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for accounts_type")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q accountsTypeQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no accountsTypeQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from accounts_type")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for accounts_type")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o AccountsTypeSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(accountsTypeBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), accountsTypePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"accounts_type\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, accountsTypePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from accountsType slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for accounts_type")
	}

	if len(accountsTypeAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *AccountsType) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindAccountsType(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *AccountsTypeSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := AccountsTypeSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), accountsTypePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"accounts_type\".* FROM \"accounts_type\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, accountsTypePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in AccountsTypeSlice")
	}

	*o = slice

	return nil
}

// AccountsTypeExists checks if the AccountsType row exists.
func AccountsTypeExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"accounts_type\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if accounts_type exists")
	}

	return exists, nil
}

// Exists checks if the AccountsType row exists.
func (o *AccountsType) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return AccountsTypeExists(ctx, exec, o.ID)
}
