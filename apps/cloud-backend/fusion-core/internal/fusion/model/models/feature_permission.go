// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// FeaturePermission is an object representing the database table.
type FeaturePermission struct {
	ID                int       `boil:"id" json:"id" toml:"id" yaml:"id"`
	FeatureID         int       `boil:"feature_id" json:"feature_id" toml:"feature_id" yaml:"feature_id"`
	AccountTypeRoleID int       `boil:"account_type_role_id" json:"account_type_role_id" toml:"account_type_role_id" yaml:"account_type_role_id"`
	AccessLevelID     int       `boil:"access_level_id" json:"access_level_id" toml:"access_level_id" yaml:"access_level_id"`
	CreatedAt         null.Time `boil:"created_at" json:"created_at,omitempty" toml:"created_at" yaml:"created_at,omitempty"`

	R *featurePermissionR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L featurePermissionL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var FeaturePermissionColumns = struct {
	ID                string
	FeatureID         string
	AccountTypeRoleID string
	AccessLevelID     string
	CreatedAt         string
}{
	ID:                "id",
	FeatureID:         "feature_id",
	AccountTypeRoleID: "account_type_role_id",
	AccessLevelID:     "access_level_id",
	CreatedAt:         "created_at",
}

var FeaturePermissionTableColumns = struct {
	ID                string
	FeatureID         string
	AccountTypeRoleID string
	AccessLevelID     string
	CreatedAt         string
}{
	ID:                "feature_permission.id",
	FeatureID:         "feature_permission.feature_id",
	AccountTypeRoleID: "feature_permission.account_type_role_id",
	AccessLevelID:     "feature_permission.access_level_id",
	CreatedAt:         "feature_permission.created_at",
}

// Generated where

var FeaturePermissionWhere = struct {
	ID                whereHelperint
	FeatureID         whereHelperint
	AccountTypeRoleID whereHelperint
	AccessLevelID     whereHelperint
	CreatedAt         whereHelpernull_Time
}{
	ID:                whereHelperint{field: "\"feature_permission\".\"id\""},
	FeatureID:         whereHelperint{field: "\"feature_permission\".\"feature_id\""},
	AccountTypeRoleID: whereHelperint{field: "\"feature_permission\".\"account_type_role_id\""},
	AccessLevelID:     whereHelperint{field: "\"feature_permission\".\"access_level_id\""},
	CreatedAt:         whereHelpernull_Time{field: "\"feature_permission\".\"created_at\""},
}

// FeaturePermissionRels is where relationship names are stored.
var FeaturePermissionRels = struct {
	AccessLevel     string
	AccountTypeRole string
	Feature         string
}{
	AccessLevel:     "AccessLevel",
	AccountTypeRole: "AccountTypeRole",
	Feature:         "Feature",
}

// featurePermissionR is where relationships are stored.
type featurePermissionR struct {
	AccessLevel     *AccessLevel     `boil:"AccessLevel" json:"AccessLevel" toml:"AccessLevel" yaml:"AccessLevel"`
	AccountTypeRole *AccountTypeRole `boil:"AccountTypeRole" json:"AccountTypeRole" toml:"AccountTypeRole" yaml:"AccountTypeRole"`
	Feature         *Feature         `boil:"Feature" json:"Feature" toml:"Feature" yaml:"Feature"`
}

// NewStruct creates a new relationship struct
func (*featurePermissionR) NewStruct() *featurePermissionR {
	return &featurePermissionR{}
}

func (o *FeaturePermission) GetAccessLevel() *AccessLevel {
	if o == nil {
		return nil
	}

	return o.R.GetAccessLevel()
}

func (r *featurePermissionR) GetAccessLevel() *AccessLevel {
	if r == nil {
		return nil
	}

	return r.AccessLevel
}

func (o *FeaturePermission) GetAccountTypeRole() *AccountTypeRole {
	if o == nil {
		return nil
	}

	return o.R.GetAccountTypeRole()
}

func (r *featurePermissionR) GetAccountTypeRole() *AccountTypeRole {
	if r == nil {
		return nil
	}

	return r.AccountTypeRole
}

func (o *FeaturePermission) GetFeature() *Feature {
	if o == nil {
		return nil
	}

	return o.R.GetFeature()
}

func (r *featurePermissionR) GetFeature() *Feature {
	if r == nil {
		return nil
	}

	return r.Feature
}

// featurePermissionL is where Load methods for each relationship are stored.
type featurePermissionL struct{}

var (
	featurePermissionAllColumns            = []string{"id", "feature_id", "account_type_role_id", "access_level_id", "created_at"}
	featurePermissionColumnsWithoutDefault = []string{"feature_id", "account_type_role_id", "access_level_id"}
	featurePermissionColumnsWithDefault    = []string{"id", "created_at"}
	featurePermissionPrimaryKeyColumns     = []string{"id"}
	featurePermissionGeneratedColumns      = []string{}
)

type (
	// FeaturePermissionSlice is an alias for a slice of pointers to FeaturePermission.
	// This should almost always be used instead of []FeaturePermission.
	FeaturePermissionSlice []*FeaturePermission
	// FeaturePermissionHook is the signature for custom FeaturePermission hook methods
	FeaturePermissionHook func(context.Context, boil.ContextExecutor, *FeaturePermission) error

	featurePermissionQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	featurePermissionType                 = reflect.TypeOf(&FeaturePermission{})
	featurePermissionMapping              = queries.MakeStructMapping(featurePermissionType)
	featurePermissionPrimaryKeyMapping, _ = queries.BindMapping(featurePermissionType, featurePermissionMapping, featurePermissionPrimaryKeyColumns)
	featurePermissionInsertCacheMut       sync.RWMutex
	featurePermissionInsertCache          = make(map[string]insertCache)
	featurePermissionUpdateCacheMut       sync.RWMutex
	featurePermissionUpdateCache          = make(map[string]updateCache)
	featurePermissionUpsertCacheMut       sync.RWMutex
	featurePermissionUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var featurePermissionAfterSelectMu sync.Mutex
var featurePermissionAfterSelectHooks []FeaturePermissionHook

var featurePermissionBeforeInsertMu sync.Mutex
var featurePermissionBeforeInsertHooks []FeaturePermissionHook
var featurePermissionAfterInsertMu sync.Mutex
var featurePermissionAfterInsertHooks []FeaturePermissionHook

var featurePermissionBeforeUpdateMu sync.Mutex
var featurePermissionBeforeUpdateHooks []FeaturePermissionHook
var featurePermissionAfterUpdateMu sync.Mutex
var featurePermissionAfterUpdateHooks []FeaturePermissionHook

var featurePermissionBeforeDeleteMu sync.Mutex
var featurePermissionBeforeDeleteHooks []FeaturePermissionHook
var featurePermissionAfterDeleteMu sync.Mutex
var featurePermissionAfterDeleteHooks []FeaturePermissionHook

var featurePermissionBeforeUpsertMu sync.Mutex
var featurePermissionBeforeUpsertHooks []FeaturePermissionHook
var featurePermissionAfterUpsertMu sync.Mutex
var featurePermissionAfterUpsertHooks []FeaturePermissionHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *FeaturePermission) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range featurePermissionAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *FeaturePermission) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range featurePermissionBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *FeaturePermission) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range featurePermissionAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *FeaturePermission) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range featurePermissionBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *FeaturePermission) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range featurePermissionAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *FeaturePermission) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range featurePermissionBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *FeaturePermission) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range featurePermissionAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *FeaturePermission) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range featurePermissionBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *FeaturePermission) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range featurePermissionAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddFeaturePermissionHook registers your hook function for all future operations.
func AddFeaturePermissionHook(hookPoint boil.HookPoint, featurePermissionHook FeaturePermissionHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		featurePermissionAfterSelectMu.Lock()
		featurePermissionAfterSelectHooks = append(featurePermissionAfterSelectHooks, featurePermissionHook)
		featurePermissionAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		featurePermissionBeforeInsertMu.Lock()
		featurePermissionBeforeInsertHooks = append(featurePermissionBeforeInsertHooks, featurePermissionHook)
		featurePermissionBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		featurePermissionAfterInsertMu.Lock()
		featurePermissionAfterInsertHooks = append(featurePermissionAfterInsertHooks, featurePermissionHook)
		featurePermissionAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		featurePermissionBeforeUpdateMu.Lock()
		featurePermissionBeforeUpdateHooks = append(featurePermissionBeforeUpdateHooks, featurePermissionHook)
		featurePermissionBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		featurePermissionAfterUpdateMu.Lock()
		featurePermissionAfterUpdateHooks = append(featurePermissionAfterUpdateHooks, featurePermissionHook)
		featurePermissionAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		featurePermissionBeforeDeleteMu.Lock()
		featurePermissionBeforeDeleteHooks = append(featurePermissionBeforeDeleteHooks, featurePermissionHook)
		featurePermissionBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		featurePermissionAfterDeleteMu.Lock()
		featurePermissionAfterDeleteHooks = append(featurePermissionAfterDeleteHooks, featurePermissionHook)
		featurePermissionAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		featurePermissionBeforeUpsertMu.Lock()
		featurePermissionBeforeUpsertHooks = append(featurePermissionBeforeUpsertHooks, featurePermissionHook)
		featurePermissionBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		featurePermissionAfterUpsertMu.Lock()
		featurePermissionAfterUpsertHooks = append(featurePermissionAfterUpsertHooks, featurePermissionHook)
		featurePermissionAfterUpsertMu.Unlock()
	}
}

// One returns a single featurePermission record from the query.
func (q featurePermissionQuery) One(ctx context.Context, exec boil.ContextExecutor) (*FeaturePermission, error) {
	o := &FeaturePermission{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for feature_permission")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all FeaturePermission records from the query.
func (q featurePermissionQuery) All(ctx context.Context, exec boil.ContextExecutor) (FeaturePermissionSlice, error) {
	var o []*FeaturePermission

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to FeaturePermission slice")
	}

	if len(featurePermissionAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all FeaturePermission records in the query.
func (q featurePermissionQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count feature_permission rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q featurePermissionQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if feature_permission exists")
	}

	return count > 0, nil
}

// AccessLevel pointed to by the foreign key.
func (o *FeaturePermission) AccessLevel(mods ...qm.QueryMod) accessLevelQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.AccessLevelID),
	}

	queryMods = append(queryMods, mods...)

	return AccessLevels(queryMods...)
}

// AccountTypeRole pointed to by the foreign key.
func (o *FeaturePermission) AccountTypeRole(mods ...qm.QueryMod) accountTypeRoleQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.AccountTypeRoleID),
	}

	queryMods = append(queryMods, mods...)

	return AccountTypeRoles(queryMods...)
}

// Feature pointed to by the foreign key.
func (o *FeaturePermission) Feature(mods ...qm.QueryMod) featureQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.FeatureID),
	}

	queryMods = append(queryMods, mods...)

	return Features(queryMods...)
}

// LoadAccessLevel allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (featurePermissionL) LoadAccessLevel(ctx context.Context, e boil.ContextExecutor, singular bool, maybeFeaturePermission interface{}, mods queries.Applicator) error {
	var slice []*FeaturePermission
	var object *FeaturePermission

	if singular {
		var ok bool
		object, ok = maybeFeaturePermission.(*FeaturePermission)
		if !ok {
			object = new(FeaturePermission)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeFeaturePermission)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeFeaturePermission))
			}
		}
	} else {
		s, ok := maybeFeaturePermission.(*[]*FeaturePermission)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeFeaturePermission)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeFeaturePermission))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &featurePermissionR{}
		}
		args[object.AccessLevelID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &featurePermissionR{}
			}

			args[obj.AccessLevelID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`access_level`),
		qm.WhereIn(`access_level.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load AccessLevel")
	}

	var resultSlice []*AccessLevel
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice AccessLevel")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for access_level")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for access_level")
	}

	if len(accessLevelAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.AccessLevel = foreign
		if foreign.R == nil {
			foreign.R = &accessLevelR{}
		}
		foreign.R.FeaturePermissions = append(foreign.R.FeaturePermissions, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.AccessLevelID == foreign.ID {
				local.R.AccessLevel = foreign
				if foreign.R == nil {
					foreign.R = &accessLevelR{}
				}
				foreign.R.FeaturePermissions = append(foreign.R.FeaturePermissions, local)
				break
			}
		}
	}

	return nil
}

// LoadAccountTypeRole allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (featurePermissionL) LoadAccountTypeRole(ctx context.Context, e boil.ContextExecutor, singular bool, maybeFeaturePermission interface{}, mods queries.Applicator) error {
	var slice []*FeaturePermission
	var object *FeaturePermission

	if singular {
		var ok bool
		object, ok = maybeFeaturePermission.(*FeaturePermission)
		if !ok {
			object = new(FeaturePermission)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeFeaturePermission)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeFeaturePermission))
			}
		}
	} else {
		s, ok := maybeFeaturePermission.(*[]*FeaturePermission)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeFeaturePermission)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeFeaturePermission))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &featurePermissionR{}
		}
		args[object.AccountTypeRoleID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &featurePermissionR{}
			}

			args[obj.AccountTypeRoleID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`account_type_role`),
		qm.WhereIn(`account_type_role.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load AccountTypeRole")
	}

	var resultSlice []*AccountTypeRole
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice AccountTypeRole")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for account_type_role")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for account_type_role")
	}

	if len(accountTypeRoleAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.AccountTypeRole = foreign
		if foreign.R == nil {
			foreign.R = &accountTypeRoleR{}
		}
		foreign.R.FeaturePermissions = append(foreign.R.FeaturePermissions, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.AccountTypeRoleID == foreign.ID {
				local.R.AccountTypeRole = foreign
				if foreign.R == nil {
					foreign.R = &accountTypeRoleR{}
				}
				foreign.R.FeaturePermissions = append(foreign.R.FeaturePermissions, local)
				break
			}
		}
	}

	return nil
}

// LoadFeature allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (featurePermissionL) LoadFeature(ctx context.Context, e boil.ContextExecutor, singular bool, maybeFeaturePermission interface{}, mods queries.Applicator) error {
	var slice []*FeaturePermission
	var object *FeaturePermission

	if singular {
		var ok bool
		object, ok = maybeFeaturePermission.(*FeaturePermission)
		if !ok {
			object = new(FeaturePermission)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeFeaturePermission)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeFeaturePermission))
			}
		}
	} else {
		s, ok := maybeFeaturePermission.(*[]*FeaturePermission)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeFeaturePermission)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeFeaturePermission))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &featurePermissionR{}
		}
		args[object.FeatureID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &featurePermissionR{}
			}

			args[obj.FeatureID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`feature`),
		qm.WhereIn(`feature.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Feature")
	}

	var resultSlice []*Feature
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Feature")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for feature")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for feature")
	}

	if len(featureAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Feature = foreign
		if foreign.R == nil {
			foreign.R = &featureR{}
		}
		foreign.R.FeaturePermissions = append(foreign.R.FeaturePermissions, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.FeatureID == foreign.ID {
				local.R.Feature = foreign
				if foreign.R == nil {
					foreign.R = &featureR{}
				}
				foreign.R.FeaturePermissions = append(foreign.R.FeaturePermissions, local)
				break
			}
		}
	}

	return nil
}

// SetAccessLevel of the featurePermission to the related item.
// Sets o.R.AccessLevel to related.
// Adds o to related.R.FeaturePermissions.
func (o *FeaturePermission) SetAccessLevel(ctx context.Context, exec boil.ContextExecutor, insert bool, related *AccessLevel) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"feature_permission\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"access_level_id"}),
		strmangle.WhereClause("\"", "\"", 2, featurePermissionPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.AccessLevelID = related.ID
	if o.R == nil {
		o.R = &featurePermissionR{
			AccessLevel: related,
		}
	} else {
		o.R.AccessLevel = related
	}

	if related.R == nil {
		related.R = &accessLevelR{
			FeaturePermissions: FeaturePermissionSlice{o},
		}
	} else {
		related.R.FeaturePermissions = append(related.R.FeaturePermissions, o)
	}

	return nil
}

// SetAccountTypeRole of the featurePermission to the related item.
// Sets o.R.AccountTypeRole to related.
// Adds o to related.R.FeaturePermissions.
func (o *FeaturePermission) SetAccountTypeRole(ctx context.Context, exec boil.ContextExecutor, insert bool, related *AccountTypeRole) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"feature_permission\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"account_type_role_id"}),
		strmangle.WhereClause("\"", "\"", 2, featurePermissionPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.AccountTypeRoleID = related.ID
	if o.R == nil {
		o.R = &featurePermissionR{
			AccountTypeRole: related,
		}
	} else {
		o.R.AccountTypeRole = related
	}

	if related.R == nil {
		related.R = &accountTypeRoleR{
			FeaturePermissions: FeaturePermissionSlice{o},
		}
	} else {
		related.R.FeaturePermissions = append(related.R.FeaturePermissions, o)
	}

	return nil
}

// SetFeature of the featurePermission to the related item.
// Sets o.R.Feature to related.
// Adds o to related.R.FeaturePermissions.
func (o *FeaturePermission) SetFeature(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Feature) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"feature_permission\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"feature_id"}),
		strmangle.WhereClause("\"", "\"", 2, featurePermissionPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.FeatureID = related.ID
	if o.R == nil {
		o.R = &featurePermissionR{
			Feature: related,
		}
	} else {
		o.R.Feature = related
	}

	if related.R == nil {
		related.R = &featureR{
			FeaturePermissions: FeaturePermissionSlice{o},
		}
	} else {
		related.R.FeaturePermissions = append(related.R.FeaturePermissions, o)
	}

	return nil
}

// FeaturePermissions retrieves all the records using an executor.
func FeaturePermissions(mods ...qm.QueryMod) featurePermissionQuery {
	mods = append(mods, qm.From("\"feature_permission\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"feature_permission\".*"})
	}

	return featurePermissionQuery{q}
}

// FindFeaturePermission retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindFeaturePermission(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*FeaturePermission, error) {
	featurePermissionObj := &FeaturePermission{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"feature_permission\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, featurePermissionObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from feature_permission")
	}

	if err = featurePermissionObj.doAfterSelectHooks(ctx, exec); err != nil {
		return featurePermissionObj, err
	}

	return featurePermissionObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *FeaturePermission) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no feature_permission provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(featurePermissionColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	featurePermissionInsertCacheMut.RLock()
	cache, cached := featurePermissionInsertCache[key]
	featurePermissionInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			featurePermissionAllColumns,
			featurePermissionColumnsWithDefault,
			featurePermissionColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(featurePermissionType, featurePermissionMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(featurePermissionType, featurePermissionMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"feature_permission\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"feature_permission\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into feature_permission")
	}

	if !cached {
		featurePermissionInsertCacheMut.Lock()
		featurePermissionInsertCache[key] = cache
		featurePermissionInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the FeaturePermission.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *FeaturePermission) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	featurePermissionUpdateCacheMut.RLock()
	cache, cached := featurePermissionUpdateCache[key]
	featurePermissionUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			featurePermissionAllColumns,
			featurePermissionPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update feature_permission, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"feature_permission\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, featurePermissionPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(featurePermissionType, featurePermissionMapping, append(wl, featurePermissionPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update feature_permission row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for feature_permission")
	}

	if !cached {
		featurePermissionUpdateCacheMut.Lock()
		featurePermissionUpdateCache[key] = cache
		featurePermissionUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q featurePermissionQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for feature_permission")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for feature_permission")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o FeaturePermissionSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), featurePermissionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"feature_permission\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, featurePermissionPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in featurePermission slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all featurePermission")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *FeaturePermission) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("models: no feature_permission provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(featurePermissionColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	featurePermissionUpsertCacheMut.RLock()
	cache, cached := featurePermissionUpsertCache[key]
	featurePermissionUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			featurePermissionAllColumns,
			featurePermissionColumnsWithDefault,
			featurePermissionColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			featurePermissionAllColumns,
			featurePermissionPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert feature_permission, could not build update column list")
		}

		ret := strmangle.SetComplement(featurePermissionAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(featurePermissionPrimaryKeyColumns) == 0 {
				return errors.New("models: unable to upsert feature_permission, could not build conflict column list")
			}

			conflict = make([]string, len(featurePermissionPrimaryKeyColumns))
			copy(conflict, featurePermissionPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"feature_permission\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(featurePermissionType, featurePermissionMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(featurePermissionType, featurePermissionMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert feature_permission")
	}

	if !cached {
		featurePermissionUpsertCacheMut.Lock()
		featurePermissionUpsertCache[key] = cache
		featurePermissionUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single FeaturePermission record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *FeaturePermission) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no FeaturePermission provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), featurePermissionPrimaryKeyMapping)
	sql := "DELETE FROM \"feature_permission\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from feature_permission")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for feature_permission")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q featurePermissionQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no featurePermissionQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from feature_permission")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for feature_permission")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o FeaturePermissionSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(featurePermissionBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), featurePermissionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"feature_permission\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, featurePermissionPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from featurePermission slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for feature_permission")
	}

	if len(featurePermissionAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *FeaturePermission) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindFeaturePermission(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *FeaturePermissionSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := FeaturePermissionSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), featurePermissionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"feature_permission\".* FROM \"feature_permission\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, featurePermissionPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in FeaturePermissionSlice")
	}

	*o = slice

	return nil
}

// FeaturePermissionExists checks if the FeaturePermission row exists.
func FeaturePermissionExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"feature_permission\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if feature_permission exists")
	}

	return exists, nil
}

// Exists checks if the FeaturePermission row exists.
func (o *FeaturePermission) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return FeaturePermissionExists(ctx, exec, o.ID)
}
