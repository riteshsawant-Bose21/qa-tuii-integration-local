project:
  name: cloud-backend
  description: Fusion Cloud backend services

tasks:
  build-all:
    deps:
      - "fusion-core:build"
    description: Build all cloud backend services and publish docker images to ECR registry
